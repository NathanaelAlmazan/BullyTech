from bullytech.models import BullyTech

# Sample model usage

model = BullyTech()

output = model.predict("Can they just send Kat and Andre home anyway because they're rude and annoying. Although, they are still my dearest friends.")

print(output)